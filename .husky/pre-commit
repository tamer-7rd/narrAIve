# Запускаем lint-staged (сторонняя библиотека): проверяет и исправляет (ESLint/Prettier) - задано нами (но сама команда из библиотеки)
# только staged файлы (те, что в git add), используя конфиг из package.json. (правило библиотеки)
# Это быстрее, чем проверять весь проект.
pnpm exec lint-staged